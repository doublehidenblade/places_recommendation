{
  "name": "poi-functions",
  "description": "Firebase Functions v2 for POI API",
  "engines": {
    "node": "20"
  },
  "type": "commonjs",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "deploy": "npm run build && firebase deploy --only functions",
    "start": "npm run build && firebase emulators:start"
  },
  "dependencies": {
    "firebase-functions": "^5.0.1",
    "node-fetch": "^3.3.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  }
}